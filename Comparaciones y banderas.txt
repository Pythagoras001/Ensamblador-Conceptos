Comparaciones y banderas

Para comparar valores se usa la instrucción:

cmp

////////////////////////////////////////////////////

Ejemplo:

cmp eax,ebx

Esto no guarda el resultado, solo actualiza unas banderas internas del procesador llamadas flags.

Luego se usan instrucciones de salto condicional.
